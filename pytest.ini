[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: testy jednostkowe (bez sieci)
    integration: testy integracyjne (pliki, pipeline)
    regression: testy regresyjne discovery/filtr├│w
    api_live: testy na ┼╝ywo API (Serper, Anthropic) ÔÇö wymagaj─ů kluczy
